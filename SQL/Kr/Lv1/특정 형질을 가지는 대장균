SELECT COUNT(*) AS COUNT
FROM ECOLI_DATA
WHERE (LENGTH(BIN(GENOTYPE)) < 2 OR SUBSTRING(BIN(GENOTYPE), -2, 1) = '0')
    AND (SUBSTRING(BIN(GENOTYPE), -1, 1) = '1' 
    OR (LENGTH(BIN(GENOTYPE)) >= 3 AND SUBSTRING(BIN(GENOTYPE), -3, 1) = '1'))